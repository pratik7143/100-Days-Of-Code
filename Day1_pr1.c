#include<stdio.h>

int main(){
    int num1,num2,sum;
    printf("Enter the number a: ");
    scanf("%d", &num1);
    printf("Enter the number b: ");
    scanf("%d", &num2);
    printf("The sum of the numbers is %d and %d",num1,num2, sum = num1 + num2);
    return 0;
}

