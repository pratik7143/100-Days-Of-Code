#include<stdio.h>

int main(){
    int a,b,sum;
    printf("Enter the number a: ");
    scanf("%d", &a);
    printf("Enter the number b: ");
    scanf("%d", &b);
    printf("The sum of the numbers is %d and %d",a,b, sum = a + b);
    return 0;
}
